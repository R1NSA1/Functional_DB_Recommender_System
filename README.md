# Functional_DB_Recommender_System

Laboratory work 5 for the recommender systems course.

## Local Setup

```powershell
uv sync
.\.venv\Scripts\activate
```

The Lab 5 datasets are stored locally in `Lab_5/datasets/` and are not committed to Git because some files exceed GitHub size limits.
